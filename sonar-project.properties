sonar.projectKey=Bew-P_Minicase
sonar.organization=bew-p
sonar.language=java
sonar.java.binaries=out  # Ensure compiled binaries are in 'out'
sonar.sources=src        # Set source code directory (adjust if needed)
sonar.exclusions=**/test/**,**/docs/**  # Only exclude tests & docs, NOT all Java files
sonar.coverage.exclusions=**/test/**,**/docs/**
sonar.qualitygate.wait=true  # Ensure SonarCloud waits for results

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=Minicase
#sonar.projectVersion=1.0


# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
